const config = {
    screenWidth: window.innerWidth,
    screenHeight: window.innerHeight,
    initialGameState: 'menu',
    fps: 60,
    musicVolume: 0.5,
    sfxVolume: 0.5,
    coinSpawnRate: 180, // Frames between coin spawns
    healthSpawnRate: 600, // Frames between health spawns
    invincibilityDuration: 600, // Frames for invincibility
    laserSpawnRate: 120, // Frames between laser spawns
};

export default config;